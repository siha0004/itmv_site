---
const { show, heading } = Astro.props;
---

<article>
  <h3 class={show}>{heading}</h3>
  <slot />
</article>

<style>
  article {
    margin: 3rem 1rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .hide {
    display: none;
  }
</style>
